---
description: 方法级注释需写清职责与执行逻辑
alwaysApply: true
---

# 方法级备注标准（DAgents）

适用于所有新增或修改的方法（含私有方法、静态方法、类方法）。

- **必须写方法级 docstring**：不要只写“做什么”，要写“怎么做”。
- **结构要求**：
  - 1) 方法职责（这个方法的目标）
  - 2) 执行顺序（关键步骤，建议 1/2/3）
  - 3) 关键分支或边界（上限、空值、异常、重试、退出条件）
  - 4) 与外部交互（队列、模型、文件、网络、数据库等）
- **异常说明**：若方法会吞异常、转换异常或向上抛出，必须在注释中说明。
- **副作用说明**：若会修改对象状态（如缓存、队列、事件、全局变量），需明确写出。
- **参数/返回值**：复杂方法需补充 `Args/Returns`（简单方法可省略 Returns）。
- **语言与风格**：中文简体、简洁明确，避免空泛描述（如“处理数据”）。

示例（推荐）：

```python
def submit_message(...):
    \"\"\"向指定 session 投递消息。

    逻辑：
    1. 获取或创建 session 队列；
    2. 构造 envelope；
    3. 按优先级入队；
    4. 队列满/会话超限时向上抛异常。
    \"\"\"
```
