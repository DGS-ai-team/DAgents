---
description: Agent tool 函数 docstring 简明规范
alwaysApply: true
---

# Agent Tool Docstring 简明规范

适用范围：使用 `@tool(...)` 暴露给 Agent 的函数。

- docstring 只保留以下 4 部分，按顺序书写：
  1) 使用场景（何时用/不该何时用，可 1-2 句）
  2) 字段说明（参数含义、类型要点、默认值）
  3) 返回说明（成功返回结构与失败返回约定）
  4) 调用范例（1-2 个，覆盖常用与边界）
- 语言要求：中文简体，短句，避免大段背景说明。
- 禁止项：冗长实现细节、泛化废话、与函数无关的架构描述。
- 若参数较多，字段说明优先写“必填/可选 + 默认值 + 约束”。

推荐模板：

```python
@tool("xxx")
def xxx(a: str, b: int = 10) -> str:
    """使用场景：用于 ...（1-2 句）。

    字段说明：
    - a: ...（必填）
    - b: ...（可选，默认 10，范围 ...）

    返回说明：
    - 成功：...
    - 失败：返回 `ERROR: ...`

    调用范例：
    - `xxx({"a":"demo"})`
    - `xxx({"a":"demo","b":20})`
    """
```
