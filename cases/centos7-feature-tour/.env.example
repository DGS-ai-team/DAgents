# 复制为本目录 .env 后按需填写（.env 已 gitignore，勿提交密钥）
# cp .env.example .env

# Compose 变量替换 + 容器环境
DAGENTS_PORT=18765

# Node LLM：true=mock（默认）；false=真实 API（需填写 OPENAI_API_KEY）
LLM_MOCK=true

# 注入 dagents-node 容器；config.yaml 中 api_key_env: OPENAI_API_KEY
OPENAI_API_KEY=
