# DAgents Node bootstrap（仅 listen/local）
# 其余设置保存在 ./.runtime/node_settings.db 与 llm_configs.db，请用 Web UI 修改。
listen:
    host: 127.0.0.1
    port: 18765
local:
    endpoint: http://127.0.0.1:18765
    node_id: ""
