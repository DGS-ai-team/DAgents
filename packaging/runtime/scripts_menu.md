# `.runtime/scripts/` 索引

Agent 执行任务前可查阅本文件；新增或删除脚本时请同步更新。

| 名称 | 命令 | 说明 |
|------|------|------|
| OfficeCLI（**Windows 包**） | `officecli.exe` | Office 文档 CLI（.docx / .xlsx / .pptx）；先 `/skill load officecli` 或加载对应子 skill |

未安装到 PATH 时使用相对路径：`.runtime/scripts/officecli.exe`。
