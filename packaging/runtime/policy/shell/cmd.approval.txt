# cmd 命令审批策略：root_command=mode（首词小写匹配）
# 管道/链式 `&` `&&` `||` 各段分别匹配；任一段需审批则整句需审批。

# --- never：只读、环境信息 ---
cd=never
chdir=never
dir=never
echo=never
type=never
more=never
find=rule
where=never
set=never
setlocal=never
endlocal=never
ver=never
vol=never
date=never
time=never
title=never
prompt=never
path=never
assoc=never
ftype=never
help=never
cls=never
color=never
mode=never
tree=never
fc=never
doskey=never
hostname=never
whoami=never
systeminfo=never
tasklist=never
driverquery=never
ipconfig=never
getmac=never
arp=never
nslookup=never
ping=never
tracert=never
pathping=never
netstat=never

# --- always：写删、注册表、网络配置、提权、脚本宿主 ---
copy=always
move=always
del=always
erase=always
rd=always
rmdir=always
md=always
mkdir=always
ren=always
rename=always
replace=always
xcopy=always
robocopy=always
attrib=always
cacls=always
icacls=always
takeown=always
mklink=always
reg=always
regedit=always
regsvr32=always
rundll32=always
msiexec=always
schtasks=always
at=always
sc=always
net=always
netsh=always
route=always
firewall=always
bcdedit=always
diskpart=always
format=always
chkdsk=always
cipher=always
compact=always
fsutil=always
powercfg=always
shutdown=always
restart=always
logoff=always
taskkill=always
tskill=always
start=always
call=always
cmd=always
powershell=always
pwsh=always
wmic=always
wsl=always
curl=always
bitsadmin=always
certutil=always
ftp=always
telnet=always
openfiles=always
runas=always
