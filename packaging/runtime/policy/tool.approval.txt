# 工具审批策略：tool_name=mode
# mode: always（必须审批） / never（免审批） / rule（走 shell 子策略，bash_run 专用）
# 未列出的工具默认为 rule（当前实现：除 bash_run 外均需要审批）。

# --- 免审批：只读或低风险 ---
read_file=never
search_file=never
host_platform=never
ask_user_information=never
agent_discover=never
load_skills=never

# --- 必须审批：写盘、改会话、跨 Agent、执行 shell ---
write_file=always
search_replace=always
bash_run=rule
agent_send_message=always
agent_broadcast=always
agent_peer_approve_tools=always
