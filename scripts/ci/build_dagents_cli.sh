#!/usr/bin/env bash
# dagents-cli (Python Textual TUI) was removed in Phase 4.
# Human UI is Web UI only; use dagents-node + embedded /ui/.
echo "[build_dagents_cli] removed in Phase 4 (dagents-cli / Textual TUI); use Web UI instead" >&2
exit 1
