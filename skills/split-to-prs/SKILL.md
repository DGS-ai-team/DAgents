---
name: split-to-prs
description: 把当前一大坨改动拆分为可评审的小 PR。
enabled: true
---

当用户希望把当前改动拆成多个 PR 时，遵循以下流程执行：

1. 先读取当前仓库状态，识别可以独立评审的切片；
2. 先给出拆分方案并等待用户确认；
3. 未获确认前，不创建分支、不提交、不推送、不建 PR；
4. 拆分执行时只按明确文件或明确 hunk 提交，避免 `git add .`；
5. 严禁破坏性命令（如 `reset --hard`、`clean -fdx`、强推）；
6. 最终输出每个 PR 的标题、链接与剩余未处理项。
