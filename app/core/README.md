# `app/core/`

> **【已弃用】** Python Agent turn loop 与编排。Go 等价：**`node/internal/turn/`**。

| 子目录 | 说明 |
|--------|------|
| **`main_agent/`** | 主 Agent 的模型与提示词：**`model.py`**、**`prompt.py`**（详见该目录 **README**） |
| **`summary_agent/`** | 上下文压缩 Agent：**`agent.py`**（summary 静默/阻塞压缩） |
