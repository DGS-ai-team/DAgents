# 06 · 运维与案例

> 本章是旧手册的兼容入口。当前启动、构建、测试和发布流程统一维护在 [`docs/development.md`](../development.md) 与 [`docs/user/operations.md`](../user/operations.md)。

## 现行入口

| 任务 | 文档 |
|---|---|
| 源码启动 Node | [`docs/development.md`](../development.md) |
| 启动 Manage / 工作组 | [`docs/user/workgroups.md`](../user/workgroups.md) |
| 安装、升级、配置和排障 | [`docs/user/operations.md`](../user/operations.md) |
| API、SSE、配置和 Schema | [`docs/reference/README.md`](../reference/README.md) |
| CI 与回归 | [`docs/development.md`](../development.md) |
| 老环境案例 | [`cases/README.md`](../../cases/README.md) |
| 版本记录 | [`CHANGELOG.md`](../../CHANGELOG.md) |

## 最小启动

```bash
cp packaging/agent-client/config.example.yaml packaging/agent-client/config.yaml
npm run build --prefix node/webui/frontend
go run ./node/cmd/dagents-node -config packaging/agent-client/config.yaml
```

工作组需要另外构建 Manage Console 并启动 `python3 run_manage.py`；Node 通过 `manage.enabled` 主动连接 Manage。Windows 没有 bash 时使用对应的 `npm run build --prefix ...` 命令。

## 文档边界

本兼容页不再维护旧 TUI、A2A inbox、Placement 或版本分期。历史运维矩阵和一次性验收材料见 [`docs/archive/README.md`](../archive/README.md)。
