# 00 · 导读

## 本章回答什么问题

读完本章，你应能：

- 知道本手册的组织方式与阅读顺序  
- 理解 DAgents 高频术语  
- 根据角色选择一条读者路径  
- 从手册章节定位到 `node/`、`client/`、`manage/` 源码目录  

---

## 1. 手册是什么

**DAgents 项目手册**（`docs/handbook/`）是仓库**唯一**技术文档正文入口。深度专题见 `docs/architecture/`、`docs/design/`；本手册负责导航与主干正文。

设计原则：

| 原则 | 说明 |
|------|------|
| **核心 → 外围** | 先 Agent Node 运行时，再 Client API，再 Manage/A2A，最后运维与案例 |
| **能读源码** | 每章给出「概念 → 文件」索引；细节以 `node/internal/*/REFERENCE.md` 为准 |
| **全中文目录** | 章节文件名与标题均为中文；代码符号、HTTP 路径保持英文 |
| **单源维护** | 架构变更先改手册对应章，再改模块 README；避免在多处重复同主题正文 |

当前发布版本见根 [CHANGELOG.md](../../CHANGELOG.md) 与手册 [README.md](./README.md) 首页。

---

## 2. 术语速览

完整定义见 [附录/术语表.md](./附录/术语表.md)。以下为开卷必备：

| 术语 | 含义 |
|------|------|
| **Agent Node** | Go 进程 `dagents-node`；多 Agent、LLM turn、工具、SQLite；内嵌 Web UI `/ui/` |
| **Web UI** | 默认人机入口；创建 Agent、对话、设置、工作组 |
| **Manage** | Python 控制面；Registry、**Workgroup** Leader、Console `/console/` |
| **`node_id`** | Node 进程身份（注册 / ACL） |
| **`agent_id`** | 本机 Agent 实例 id（1 Agent = 1 主对话） |
| **Workgroup / Member** | 跨 Node 协作单元；成员在 Home Node 工作区执行工具 |
| **turn** | 一次模型步（可能含 tool batch）；直到 `done` 或 HITL |
| **HITL** | Human-in-the-Loop；工具审批、询问用户 |
| **子 Agent** | Node 内临时 runtime；共用进程，不单独暴露端口 |

---

## 3. 四条读者路径

### 路径 A · 30 分钟入门（联调）

```text
00-导读 → 01-愿景与架构 §1–§2.1 → 根 README 快速开始 → 打开 /ui/
```

动手：构建 Web UI → `go run ./node/cmd/dagents-node` → 浏览器 `http://127.0.0.1:18765/ui/`。

### 路径 B · 改 Node 内部

```text
02-Agent-Node-核心（§1 单次 LLM → §2 loop → §3 队列 → §4）
  → session/README.md → turn/README.md → queue/
```

建议测试：`go test ./node/internal/session/... ./node/internal/turn/...`

### 路径 C · Manage / 工作组

```text
07-Workgroup协作 → 05-Manage与A2A → design/workgroup-and-node-gateway.md
  → design/v0.9.1-smoke-checklist.md
```

### 路径 D · 发布与运维

```text
06-运维与案例 → packaging/agent-client/ → 附录/配置项参考.md
```
---

## 4. 仓库目录 ↔ 手册章节

| 目录 | 手册章节 | 职责 |
|------|----------|------|
| `node/` | Part II、IV、V（Node 侧） | Agent 运行时 |
| `node/cmd/dagents-node/` | [02](./02-Agent-Node-核心.md) §4.5 | 进程入口 |
| `node/internal/session/` | [02](./02-Agent-Node-核心.md) §3–§4 | Manager、runtime、队列 |
| `node/internal/turn/` | [02](./02-Agent-Node-核心.md) §1–§2 | Orchestrator、LLM loop |
| `node/internal/api/` | [03](./03-API与Client.md) | HTTP/SSE · workgroup 反代 |
| `node/webui/` | [03](./03-API与Client.md) | 内嵌 Web UI |
| `node/internal/tools/` | [04](./04-能力与策略.md) | 内置工具 |
| `node/internal/workgroup/` | [07](./07-Workgroup协作.md) | Workgroup Worker |
| `node/internal/manage/` | [05](./05-Manage与A2A.md) | 注册 / Dialer |
| `client/` | [03](./03-API与Client.md) | probe / update / version |
| `manage/` | [05](./05-Manage与A2A.md) · [07](./07-Workgroup协作.md) | Manage + Workgroup + Console |
| `shared/config/` | [附录/配置项参考](./附录/配置项参考.md) | 共用 YAML |
| `packaging/` | [06](./06-运维与案例.md) | 发布布局 |
| `cases/` | [06](./06-运维与案例.md) | 集成案例 |
| `tests/` | 各章测试索引 | Python 集成测 |

---

## 5. 与模块 REFERENCE 的关系

| 层级 | 位置 | 写什么 |
|------|------|--------|
| **手册** | `docs/handbook/` | 为什么、怎么串、读源码路径 |
| **包 README** | `node/internal/*/README.md` | 包职责、文件一览、协作图 |
| **REFERENCE** | `node/internal/*/REFERENCE.md` | 类型、函数、字段、配置键 |

改 API 或行为时：**REFERENCE → 包 README → 手册对应章**，顺序保证细节不丢。

---

## 6. 下一章

→ [01-愿景与架构](./01-愿景与架构.md)：项目定位、组件边界、仓库拓扑。
