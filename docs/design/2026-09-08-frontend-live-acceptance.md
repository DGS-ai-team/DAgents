# 真实前端验收与设置页布局调整

2026-09-08，负责人通过浏览器操作，Luna 编写修复和样式代码。

## 已验证

- 使用现有 `test` Agent 的 `mimo-v2.5-pro` 配置，从输入框发送计算请求，页面收到“验收结果：703”，并显示真实模型用量。
- 从长期任务表单创建单轮目标，运行次数 1，累计/单轮阈值均为 32,000 tokens。通过专用会话批准一次 `goal_checkpoint`，目标完成，证据为 `37×19=703`，实际用量 28,870 tokens。未调用其他工具或写入测试文件。
- 验收目标：`c1236f9b-cb9f-4ee1-8c8b-d7c1c53b4894`。保留该已完成记录供检查，不会继续唤醒。
- 通用设置保持原值保存，页面出现成功提示。检查通用、连接、输出防护、上下文和模型编辑弹窗的布局；浅色、深色均进行截图核对。

## 发现并修复

1. 主聊天已有较高 history revision 时，Goal 较低 revision 的快照被错误拒绝，造成主历史残留。为 transcript 增加会话归属，跨会话切换不再比较 revision，同时丢弃旧会话 SSE 事件。加入跨会话快照回归，并用现有非空主聊天重新验证切入与切回。
2. Goal 完成后残留 `approval_required`。生命周期成功结算清理过期原因，前端兼容已有终态记录；完成/停止显示“不会再运行”。

## 设置页设计

标量配置与开关采用一项一行：名称和说明在左，控件在右，统一间距与细分隔线；窄屏堆叠。覆盖共享设置面板及模型编辑表单。复杂模型列表和文本编辑区保留所需结构，不修改配置含义或认证策略。

检查结果：Node 前端 58 文件、319 项测试通过；Go goals/API 包通过；Node UI 构建通过。

本次新版前端预览在 `127.0.0.1:18767`，连接已有 `18765` Node。未重启用户的 Node/Manage。后端生命周期修复需运行新构建后生效；前端已兼容旧记录。本次未覆盖所有外部工具或工作组故障场景。
# Auto Agent 模型补充验收（2026-09-08）

按用户纠正的两类 Agent 模型，在独立临时运行目录与 Node 18766、前端 18768 中验收；复用本机已配置的真实 `mimo-v2.5-pro` 配置，未修改原有 Agent。

- 通过创建向导创建 `Auto 前端真实验收`（`agt-9ea36189d2221ea1`），不授予额外文件或命令工具。
- 空闲主聊天计算 `23×17`，实际答复“聊天验收：391”。
- 在智能体设置“自主任务”中保存暂停任务，次数上限 2、总预算 32000、单轮 16000、最小间隔 60 秒；保存后 0 次运行。
- 主聊天真实调用 `autonomy_get` 和 `autonomy_update`，把目标改成 `31×13`、完成条件改成保存 `403` 及证据；设置页读回新目标，暂停状态、次数与预算不变。
- 在设置页显式启用，由调度器自动触发（运行原因 `schedule`），未使用“立即运行”。专用执行会话收到修改后的目标。
- 经一次 `goal_checkpoint` 审批后，Goal `2d4919ed-9d03-42bb-86bf-4b1dc48aa20f` 完成，运行 1 次、用量 4128 Token，证据包含 `310+93=403` 和反向验算。

配置和普通聊天用量与自动执行用量分别核算。以上是实际模型调用和浏览器操作，未把 mock 或默认跳过的测试计为真实验收。
