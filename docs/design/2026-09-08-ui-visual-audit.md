# Node / Manage UI 视觉验收（2026-09-08）

本次由主代理实际浏览页面、提出问题并复验，Luna 实现布局修改。已完成主要页面的桌面巡检，并补验关键窄屏页面与 Manage 有数据状态。下面首轮记录保留检查经过，最终覆盖与限制以文末补验结果为准；不代表全部业务状态、浏览器或主题组合均已通过。

## 验收环境

- Node 当前前端：127.0.0.1:18767，连接原有 Node 18765，使用已有数据，只读查看。
- Auto Agent：独立验收 Node 18766 / 前端 18768，查看上一轮真实模型执行后的 Auto 设置。
- Manage 正式实例 8020 仍停在登录页，未修改管理员凭据。
- Manage 独立前端 18769 / 后端 8021，独立数据库、验收管理员，检查空状态及新建表单，并创建一条隔离工作组用于详情验收；未发布、未发起协作运行。
- 实际截图主要为约 1280×720 桌面视口。Node 浅色和 Auto 深色、Manage 浅色及部分深色页面已查看。窄屏仅审查 CSS，尚未完成真实视口验收。

## 页面覆盖

| 端 | 页面 | 实际检查状态 |
| --- | --- | --- |
| Node | 通用 | 设置首屏与行布局 |
| Node | 模型与连接 | 已配置模型列表、Manage 连接区；此前轮次检查过模型弹窗 |
| Node | MCP | 服务列表、219 个工具展开目录；未展开敏感 JSON 配置 |
| Node | Linux 通道 | 已配置凭据/通道列表、新增 SSH 表单 |
| Node | 能力、技能、输出防护 | 配置行、配额、技能空状态；未切换会修改配置的开关 |
| Node | 上下文 | 配置行、诊断摘要及展开入口 |
| Node | 智能体列表和详情 | 列表、记忆空状态、资源绑定、工具及 Shell 审批表；基本设置和能力/技能分行在前一轮已实看 |
| Node | Auto 自主任务 | 独立真实验收 Agent 的已完成任务设置、深色布局 |
| Node | 定时任务 | 托管任务列表、新建行式表单 |
| Node | 关于与帮助 | 版本状态及全部命令分类 |
| Node | 帮助与反馈 | 提交表单布局；不提交新反馈 |
| Node | 长期任务记录 | 旧任务列表及主题样式 |
| Node | 聊天、终端 | 真实历史消息、工具卡片、表格、代码块、输入区、终端空状态与消息侧栏 |
| Node | 工作组 | 不可加载提示及未选择工作组状态；真实协作详情未通过验收 |
| Manage | 登录 | 正式实例登录页、独立实例管理员登录 |
| Manage | 首页 | 空库及一条测试工作组概览、浅色/深色 |
| Manage | Node / Agent 列表 | 筛选、空表格、分页 |
| Manage | 发现组 | 空列表、创建输入区、未选中提示 |
| Manage | 配置 / LLM | 空状态、新建弹窗及默认复选框 |
| Manage | 配置 / 版本发布 | 上传表单及发布开关；未上传 |
| Manage | 配置 / 案例库 | 空列表、新建流程第一步；未上传 JSONL |
| Manage | 用户反馈 | 筛选和全空状态、浅色/深色；有数据详情待验 |
| Manage | 工作组 | 空列表、新建表单、隔离测试工作组详情、成员新建空 Agent 状态 |
| Manage | 能力市场 | Skill / Hook / 外置工具空表格及 Skill 上传弹窗；未上传 |

## 已修复与复验

1. Node 能力与可见技能独立排列，反馈页使用统一设置宽度和分隔线（前一轮完成，本轮反馈页复看）。
2. SSH 新建表单端口与用户名改为两列，消除右半区空白；实际复验通过。
3. 定时任务新建表单改为一配置一行；托管任务显示明确归属并隐藏普通编辑操作；实际复验通过。
4. 帮助页命令分类改为纵向整宽，命令/说明约 40/60 分列；首次修复被旧 grid 规则覆盖，退回修正后实际复验通过。
5. 模型与连接页 Manage 区去除 420px 宽度限制，与页面配置行同宽；实际复验通过。
6. 长期任务历史页使用项目实际主题变量，消除未定义颜色变量；列表实看通过。
7. Manage 首页两卡改两列，移除卡片尺寸限制，间隔 24px；首次修改仍留大空隙，退回修正后实看通过。
8. Manage 顶部导航隐藏纵向溢出，消除多余小竖滚动条；实际复验通过。
9. Manage LLM 默认配置复选框恢复横向排列；实际复验通过。
10. Manage 反馈筛选统一面板、全空状态占满宽度并居中；实际复验通过。有列表未选详情新增提示。
11. Manage 反馈详情长标题允许换行、工作组窄屏操作区允许换行；源码修改及构建通过，对应长数据/窄视口仍待实看。

## 验证与剩余范围

- Node 前端测试：59 个文件、323 项通过（Luna 执行）；此后新增修改仅为 CSS，相关前端构建通过。
- Node 和 Manage 最终前端构建通过；Manage 有既有 chunk size / eval 警告。
- `git diff --check` 未发现空白错误；工作区既有大量未提交修改全部保留。
- 待正式 Manage 登录后补验：有数据的 Node/Agent 抽屉、反馈详情、发现组成员、已发布能力包与版本、案例第二步及回放、工作组对话与运行历史。
- 待补：真实窄屏视口、全页深浅主题组合、各类错误/长内容/弹窗数据状态。当前记录不得作为这些状态已通过的证明。
- 正式 Node 后端仍是原进程，Auto 后端新行为在隔离实例验证；本次没有替换正式 Node 进程。

## 补验结果（同日第二轮）

Manage 在独立 8021 数据库中加入明确标注的合成验收数据，通过 18769 前端实际浏览。没有改动正式管理员凭据或业务数据。合成注册记录不对应可用服务器；版本记录不包含可下载包；工作组运行记录用于视觉验收，不是实际协作执行证明。

| 范围 | 实际补验 |
| --- | --- |
| Manage Node / Agent | 3 条 Node、2 条 Agent 列表及详情抽屉，长描述和标签，深色/浅色代表页面 |
| Manage 发现组 | 有成员的分组列表、成员显示 |
| Manage 用户反馈 | 3 条长标题与正文、处理状态、管理员回复、详情；桌面双栏与 390px 窄屏；其他类型标签 |
| Manage 案例库 | 3 条案例列表、消息回放、编辑表单、资源绑定、消息编辑弹窗 |
| Manage 能力市场 | 已发布 Skill、Hook、外置工具目录、版本显示 |
| Manage 版本发布 | 草稿与已发布版本列表、最新标签、操作区；未测试包下载 |
| Manage 工作组 | 有成员详情、对话消息、运行历史；390px 调试面板上下布局 |
| Manage 窄屏基础页 | 首页、Node 列表、反馈、LLM 新建弹窗 |
| Node 窄屏 | 390px 模型与连接、MCP 服务及长工具名、Auto 聊天、展开/收起 Agent 导航、终端和消息区域 |
| Node 桌面回归 | 已有 Agent 聊天历史、工具卡片、表格、代码块；帮助反馈统一行布局；Auto 状态条主题 |

### 本轮发现并修复

1. Node 窄屏设置固定侧栏挤压正文：改为顶部可横向滚动导航。
2. 模型卡片按钮与内容在窄屏拥挤：操作区独立换行。
3. MCP 固定网格最小宽度和长工具名溢出：网格允许收缩，名称、描述可换行；DOM 宽度及截图复验通过。
4. 聊天与工作组导航在手机端占用正文：增加收起导航和选择 Agent 入口；Auto 聊天与终端实际复验通过。
5. Auto 状态条缺少留白且链接显示默认紫色：补齐主题颜色、边框和内边距。
6. Manage 案例资源勾选项被通用 label 样式纵向排列：显式横排，左侧复选框、右侧名称与版本；第一次修复未生效，退回后截图通过。
7. Manage 窄屏运行历史挤压聊天：改为上下区域，各自滚动；实际 390px 截图通过。
8. Manage 反馈刷新按钮拉伸：顶部对齐；补齐“其他”类型筛选与详情文案，避免显示为“问题”。
9. Node 聊天流切换条件比较对象错误：Luna 修正 conversation ID 判断，并完成 59 文件 / 323 项测试和构建。
10. Manage 顶部把 Node 与 Agent 总数误标为 nodes：改为“注册实体”，实际显示 5 个注册实体，与 3 Node + 2 Agent 一致。

### 最终验证边界

- Node 测试 323 项通过，Node 与 Manage 前端构建通过；Manage 无 npm test 脚本，构建仍有既有 chunk size / eval 警告。
- 正式 Node 曾有单个验收标签页失去响应；新建 localhost:18767 标签页可正常加载同一后端的已有聊天与实时事件。未替换正式进程，未将该标签页超时归因为已确认的业务缺陷。
- 真实模型执行证据沿用《2026-09-08-frontend-live-acceptance.md》；本轮 Manage 运行历史是合成数据，不能替代真实跨 Node 协作或发布下载验收。
- 主要页面入口已覆盖。没有穷举所有深浅主题、屏幕尺寸、错误状态和业务数据组合；正式 Manage 8020 登录后的实际业务数据仍未检查。
- 临时窄屏标签页已关闭；保留桌面验收入口用于查看。
