{
  "test": "legacy_placement_cannot_drive_member",
  "attempts": [
    "POST /v1/peers/nodes placement create",
    "A2A agent_invoke targeting mb_01",
    "Edge tunnel chat as remote agent"
  ],
  "expected": "rejected_or_noop_member_run_unchanged"
}
